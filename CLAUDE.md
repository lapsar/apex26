# APEX '26 — ядро памяти проекта

Репозиторий: `lapsar/apex26`. Основной файл — `index.html` (текущая версия **1.16.5**).

Это ЯДРО: правила, константы, запреты и указатель. Подробности (замеры, таблицы, история
решений, отвергнутые варианты целиком) лежат в `docs/notes/` — **перенесены туда из прежнего
CLAUDE.md без правки текста** (09.2026), ничего не выброшено. Ядро загружается в каждую
сессию целиком, поэтому держим его коротким, а за подробностями ходим по указателю (§16).

---

## 1. Что это

Браузерная аркадная гонка Формулы-1 от первого лица. **Один самодостаточный HTML-файл**
(~1 МБ): весь код, three.js **r128** и данные трасс — инлайн. Офлайн, без сборки, без
внешних файлов и npm в продукте. Интерфейс русский.

## 2. Дух игры

Главный ориентир. Правка, которая ему противоречит, неверна, даже если изящна.

- **Аркада, а не симулятор.** Две кнопки поворота, газ, тормоз; руль поворачивает болид
  напрямую, без физики скольжений.
- **Реальные конфигурации трасс.** Контуры настоящие, линии старта на настоящих местах,
  окружение стремится к достоверности. Ради этого проект и делается.
- **Повороты проходишь сам.** Никаких помощников, идеальных траекторий, автоторможения.
- **Ошибка стоит времени, а не гонки.** Вылетел — теряешь секунды и позиции, но можешь
  вернуться и отыграться (поэтому стены не вплотную).
- **Никто никому не уступает.** Соперники не расступаются перед игроком.
- **Контакт есть и ощутим.** Машины не проходят друг сквозь друга.
- **«Новичок проходим ребёнком ~6.5 лет» — ФАКУЛЬТАТИВНО с 09.2026** (v1.15.80, решение
  владельца: ребёнок научился). Уплотнение трафика на Новичке — факт, который надо назвать,
  но не запрет. Новичок остаётся самым лёгким режимом.

| Режим | Замысел |
|---|---|
| Новичок (по умолчанию) | Ребёнок должен доехать и бороться. Соперники медленные. |
| Норма | Основной режим для взрослого. |
| Профи | Тяжело. Обгонять в основном на прямых и в быстрых поворотах. |

## 3. Производительность

Главное устройство — **iPad mini 5** (также iPhone 16 Pro Max, iPad 5, MacBook).
**Узкое место — число вызовов отрисовки, а не полигоны.** Потолок в гонке **495 вызовов**.

| Что | Значение |
|---|---|
| Болид игрока / ИИ (упрощённый) | 32 меша 3000 тр. / 17 мешей 1332 тр. |
| Вызовов в гонке (22 болида) | Монца 492, Сильверстоун 495, Монреаль 491, Майами 494, Хунгароринг 488 |
| Мешей сцены | Монца 43, Сильверстоун 46, Монреаль 42, Майами 45, Хунгароринг 39 |

- **Склейка по материалам** (`mergeGeos`/`mergeStatics`/`mergeCar`, своя — в r128 нет
  `BufferGeometryUtils`). Переносит позиции, нормали, **UV и вершинные цвета** — новый
  атрибут добавлять туда же, иначе чёрные трибуны.
- **Трибуны стоят ноль** (все сиденья — один меш). **Каждый НОВЫЙ цвет/материал стоит
  ровно меш и вызов.** Обход — **атлас**: мотивы полосами в одной текстуре, выбор по UV
  (`RAIL_ADS` общая на всю игру, трасса берёт зоны ключом `railAds`; свободно 6 полос;
  высота атласа считается от длины таблицы; плата — треугольники и видеопамять).
- `LOD_NEAR=4`: полную модель получают 4 ближайших соперника.
- Не включать: тени от окружения, прозрачные сетки, отражения, отдельных зрителей.
  Стопки покрышек убраны (08.2026).
- Подробности: `docs/notes/performance.md`.

## 4. Баланс — священная корова

```js
MAXSPEED = 88
GRID_GAP = 8
DIFF_MUL  = { easy: 0.85, normal: 0.90, hard: 1.00 }   // доля от ПОТОЛКА игрока
DIFF_GRIP = { easy: 0.90, normal: 0.98, hard: 1.00 }   // доля от СЦЕПЛЕНИЯ игрока (v1.15.77)
CORNER_SKILL = 0.5                                     // влияние силы пилота на поворот
QUALI_PACE = 1.0075                                    // квалификация быстрее гоночного темпа (v1.15.79)
AIBRAKE = 44 (игрок 50), разгон ИИ 13.5 (игрок 14)     // оставлено разным осознанно
```

```js
base_i       = MAXSPEED * track.grip * DIFF_MUL[diff] * (0.5538 + skill*0.32) / (0.5538 + 0.98*0.32)
cornerFrac_i = DIFF_GRIP[diff] * (1 + (skill - 0.98) * CORNER_SKILL)
v_corner(R)  = aiCornerV(R) * cornerFrac     // тот же закон руля, что у игрока
```

- **Сложность — доля от ОДНОЙ машины** (v1.15.66): закон сцепления и потолок у игрока и ИИ
  общие. Соперник ограничен и поворотом, В КОТОРОМ едет (`aiTarget`, обзор с `a=0`, радиус
  `24/K+halfAt*0.8`, v1.15.77).
- **Сила пилота** (0.81–0.98, данные в `TEAMS`) правится свободно, пока максимум поля 0.98:
  якорь двигает только лидер поля.
- `DIFF_CORNERK` в игре не читается — его читает автопилот `tools/harness.js`.
- Каждый параметр лежит **в одном месте**, остальные его читают.

### Якорь: времена поула (главный тест проекта)

**После любой правки, которая не должна менять баланс, совпасть обязаны все 63 клетки
до тысячной** (7 трасс × 3 режима × P1/P11/P22). Здесь — P1; P11 и P22 лежат
в `tools/probe-pole-times.js`. Проверка: `node tools/run-all.js --only=pole`.

| Трасса | Новичок | Норма | Профи |
|---|---|---|---|
| Monza | 1:33.760 | 1:29.963 | 1:27.896 |
| Silverstone | 1:51.648 | 1:46.405 | 1:44.754 |
| Suzuka (скрыта) | 1:50.965 | 1:45.722 | 1:43.699 |
| Monaco (скрыта) | 1:26.424 | 1:22.309 | 1:21.302 |
| Montreal | 1:24.555 | 1:20.839 | 1:19.328 |
| Miami | 1:42.975 | 1:38.033 | 1:35.596 |
| Hungaroring | 1:35.720 | 1:31.404 | 1:30.227 |

Это КВАЛИФИКАЦИОННОЕ время (`qualiLapTime`, не `estLapTime` напрямую). **Формулы игры звать
по имени, а не копировать** — копии молча устаревают (справка `lap-potential` врала 7 сборок).

**Переписывать якорь можно только так** (образец — v1.15.66, .77, .79; Хунгароринг — v1.16.4, v1.16.5): (1) новые числа
посчитаны ДО правки, на нетронутом `index.html`, и код после правки выдал ровно их;
(2) якорь расширяется (P1/P11/P22), а не только поул; (3) старая таблица остаётся рядом
с пометкой версии. Прежние таблицы — `docs/notes/balance.md`.

### Что НЕ влияет на баланс

Стены и отбойники (с ними взаимодействует только игрок, ИИ их не видит); цвета, модель,
окружение, звук; выбор пилота игроком (физика игрока не содержит силы пилота — оставлено
решением владельца); нейтрализация, защёлка обгона, ошибки пилотов ИИ (`estLapTime` о них
не знает). **Но**: ошибки ИИ замедляют ГОНОЧНЫЙ темп поля на ~1 с/круг — якорь этого
не видит и не должен.

- Разброс квалификации **±0.2 %, симметрично** (`beginQuali`), сторожит пробник `quali`.
- Гоночный множитель `0.965+rnd*0.035` на решётке — другой множитель.
- Подробности (плотность поля по трассам, история якоря, запас тормозов, асимметрия разгона
  по `track.grip`): `docs/notes/balance.md`.

## 5. Нейтрализация (жёлтый флаг и VSC) — коротко

| | Когда | Где | Потолок |
|---|---|---|---|
| Жёлтый (40 с) | болид встал вне полотна | 2 маршальских сектора из 12 | 0.9 **своего** темпа |
| VSC (27+3 с) | болид встал частью на полотне | весь круг | 0.6 **своего** темпа |

- Потолок — доля от СОБСТВЕННОГО темпа каждого (v1.15.42); обгон закрыт дистанцией
  `NEUT_GAP=9` м по НАСТОЯЩЕМУ расстоянию (не разностью `dist`!), правило «не приближаться»,
  с запомненным полом и возвратом `NEUT_RECOVER=3`.
- Того, кто медленнее `NEUT_STOPPED=6` м/с (абсолютный порог), объезжать можно — иначе
  вставший запирает очередь. Относительная мерка проверена и забракована.
- Слипстрим, `OFFLINE_COST` и ошибки ИИ под флагом и на подъезде к зоне выключены.
- **Известная дыра:** `NEUT_GAP` не может ничего сделать с парой, УЖЕ идущей бок о бок
  (~1 спорный обгон на 12 заездов; на узких трассах, где `1.24*hw < 9 м` — Хунгароринг,
  Монако — пара физически не разводится на 9 м). Ждёт логики обгона (open-tasks п.16-17).
  Вторая дыра: правило сравнивает СКОРОСТИ — по внутренней стороне поворота игрок
  при равной скорости подбирается до ~6.3 м (Хунгароринг T1, старая, neutralisation.md).
- **Правка, меняющая темп ТОЛЬКО ИИ, молча ломает равенство под флагами** — проверять
  справкой `tools/neutral-audit.js` (оба режима, против прошлой сборки) В ХОДЕ работы,
  пробника `neutral` мало (слепая зона: обгоны над провалившимся заранее прощены).
- Болид после схода остаётся до финиша; после VSC маршалы убирают его за отбойник.
- Подробности: `docs/notes/neutralisation.md`.

## 6. Физика игрока: что можно и чего нельзя

```js
player.hdg -= sign(speed)*steerAmt*turn*dt
turn = min( 1.7*grip*max(0.32, min(1, 12/(|speed|+4))) ,  |speed|/TURN_RMIN )   // TURN_RMIN=10.1 м
```

- **Пределов два:** по сцеплению и по выкруту колёс (`TURN_RMIN`, v1.15.58; пересекаются
  на 45 км/ч). **0.32 — ПОЛ, а не спад** (прочитали наизнанку однажды — ошибка 2–4 с/круг).
- `steerVis` — только картинка (колёса до 20°, руль ×2.43), менять безопасно.
- Задний ход: `REV_MAX=20/3.6`, `REV_ACC=3.0`, `REV_ARM=1.5` с удержания тормоза,
  запас `REV_RANGE=10` м (метр назад тратит, метр вперёд восполняет). Решётку держит
  ОСТАНОВЛЕННАЯ физика, пока горят фонари, а не запас.
- `track.HW` (ширина полотна) — **игровой слой**, менять только с полной перекалибровкой.
  Наше полотно шире реального на всех трассах (замер TUMFTM).
- Контакт — прямоугольник `A=6.0 × B=2.5` (габарит 6.04 × 2.57).
- **Знак кривизны: `K<0` — правый поворот, `K>0` — левый.**
- **Порядок в кадре:** `физика игрока → placePlayer → placeAI → carContacts() ×2 →
  placePlayer → LOD`. Расталкивание строго ПОСЛЕ перемещения ИИ; больше двух проходов нельзя.
- `dist` у игрока (дуга по осевой) и у ИИ (пройденный путь) — **разные линейки**; зазор
  между игроком и ИИ мерить только настоящим расстоянием.
- ИИ в мире не рулит: «точка на осевой по `u` + `lane` вбок»; доворот кузова (`crab`) —
  картинка. Предел сближения ИИ — по времени до контакта (`FOLLOW_TAU=0.5`) и не быстрее
  своих тормозов (`FOLLOW_BRAKE=50`), а не присваиванием (v1.15.61).
- Подробности: `docs/notes/game.md`, `docs/notes/limitations.md`.

## 7. Контракт модели болида

`makeCar(colorHex, num, simple)` → `THREE.Group`. «Вперёд» **+Z**; y=0 — асфальт;
колёса радиус 0.5, передние x=±1.05 z=+2.0, задние z=−1.7; кузов 2.57 × 6.04 × 1.22,
высота ~1.4. `userData = { frontWheels, wheels, sw }` обязателен (`sw` только при
`simple===false`). Камера кокпита y=1.42, на 0.62 позади центра. Модель своя,
процедурная, без чужих ассетов. Полностью — `docs/notes/game.md`, `docs/BRIEF-model-bolida.md`.

## 8. Трассы

Контуры — `bacinger/f1-circuits`, вшиты в `TRACKDATA`.

| Трасса | Круг | Полуширина | sfShift | Статус |
|---|---|---|---|---|
| Monza | 5768 м | 8.0 | 5440 | видима, размечена; **закрыта для правок** (кроме просьб владельца) |
| Silverstone | 5867 м | 7.5 | 3120 | видима, размечена; **закрыта для правок** |
| Suzuka | 5805 м | 7.0 | 5690 | скрыта (плоский мост — трасса пересекает сама себя) |
| Monaco | 3320 м | 6.0 | 2460 | скрыта (нет тоннеля и арки) |
| Montreal | 4349 м | 7.0 | 196 | видима, разметка частичная (этап 3 закрыт) |
| Miami | 5424 м | 7.0 | 5274 | видима, против часовой; барьер и зоны по онбоарду, трибуны, щиты |
| Hungaroring | 4363 м | 6.0 | 4118 | видима с v1.16.3, контур на середине дороги с v1.16.4, точки сглажены v1.16.5, окружение обобщённое |

- **Перевод координат:** `lat0,lon0` = среднее точек (без дубля); `x=(lon-lon0)*111320*cos(lat0)`,
  `y=(lat-lat0)*110540`, округление 0.1 м. Все трассы ~0.4 % короче реальных — не менять.
  Сверка: `tools/hungaroring-scan/convert.py --check`.
- **Ловушки:** контур отражается по X (`V3(-p[0],0,p[1])`); `sfShift` — метры от начала
  контура до настоящей линии старта (ротирует `P/F/R/K/S`).
- **Новая трасса не должна ломать старые:** данные изолированы по ключу (`TRACKDATA`,
  `TRACKS`, `SCENERY_BY_KEY`, `STRAIGHTEN_STANDS`, `SCEN_ORIGIN` — без строки там
  построение падает). Ломает правка ОБЩЕГО кода — её ловит отпечаток (`print`).
- **Добавление трассы (кратко):** контур → `TRACKDATA`, spec в КОНЕЦ `TRACKS` с `hidden:true`;
  линия старта: на длинной прямой ищет СНИМОК, телеметрия проверяет, лучшая проверка —
  стартовая решётка из openf1 (`openf1-grid.py`); линий может быть две (старт ≠ финиш,
  решает арифметика дистанции гонки); карта сверху владельцу; чек-лист; проверить, идёт ли
  контур по середине дороги (`halfwidth.py`); спросить владельца про онбоард-видео.
  Полностью: `docs/notes/tracks/overview.md`.

### Окружение: уроки, каждый стоил итерации

1. Привязка объекта — окно ±60 м вокруг `S`, не ближайшая точка.
2. Отступ больше местного радиуса складывает ленту: прятать ленту, закрывать перемычкой;
   физика стены ОБЯЗАНА идти по той же хорде (`wallSpanFill`), зона вылета там — отдельной
   фигурой (`zoneSpanFill`).
3. Ленты слева и справа обходятся в разные стороны — плоские ленты двусторонние
   (или U развернуть на одной стороне).
4. Склейка теряет незнакомые атрибуты (UV, цвета) → чёрное.
5. Основной цвет команды — ключ в таблице вторичных цветов.
6. Трибуна не заходит внутрь отбойника; зазор мерить до ОТБОЙНИКА.
7. Умножение цвета только затемняет.
8. Комментарий `//` посреди строки данных глотает остаток объекта.
9. Зона вылета — от кромки до отбойника, без щелей травы.
- **Мерить ПОСТРОЕННУЮ геометрию, а не данные разметки:** `atS` пересчитывается из `latLon`,
  `STRAIGHTEN_STANDS` подменяет дугу хордой — проверка по данным дважды врала.
- **Щиты торможения и отбойник связаны геометрически:** правишь барьер — пересчитай щиты
  (стойка на 0.30 м ЗА отбойником). Обожглись дважды.
- `makeBasis` требует правую тройку (объект слева — разворот `dir`).
- Коробки сдвигаются наружу за барьер правилом `clearBehindWall`, а не числом в данных.
- Проверять ориентацию трибун картинкой (`tools/shot-track.js`), на однотонном фоне нельзя.
- Лишняя случайная текстура сдвигает поток `Math.random` и всю гонку — текстуры создавать
  лениво, только для реально размеченного покрытия.
- Подробности по трассам: `docs/notes/tracks/*.md`; источники данных: `docs/notes/sources.md`.

## 9. Известные ограничения (одной строкой)

Ломаная кромка в шиканах (редкий контур, шаг 4 м, сглаживать — отдельный этап; шум точек
контура ищет `contour-wobble.js`, лечит `contour-smooth.js` — у Хунгароринга сделано v1.16.5,
у скрытого Монако 21 место); контакт
считает соперника точкой; ИИ в колонне иногда заходят друг в друга на ~0.4 м, между собой
расходятся по данным, а не столкновением; `dist` — разные линейки; отбойник ступенькой
на смене знака поворота (сглажено только у Монреаля, `rail.smooth`); автопилот после финиша
выносит в быстрых поворотах; «влетаешь в зад на выходе из поворота» — арифметика режима,
не дефект; шпилька Канады не преимущество игрока; вставший игрок замирает как встал.
Полностью, с числами: `docs/notes/limitations.md`.

## 10. Открытые вопросы и решения владельца

Статусы на 09.2026 (номера — как в прежнем §10, подробности в `docs/notes/open-tasks/`):

| п. | Что | Статус | Файл |
|---|---|---|---|
| 1 | Канада: пит-лейн, мост у S 2230 | ждёт желания владельца | tracks |
| 2 | Сузука и Монако в меню | нужны мост/тоннель/арка — только онбоард | tracks |
| 4 | Борьба соседей по ходу гонки | ошибки пилотов сделаны (v1.15.75-76); компенсация признана ненужной | race-ai |
| 5–9 | Гонка без квалы; переменная ширина; Mercedes; контакт по углам; сейфти-кар | не сделано | race-ai / tracks |
| 13 | Потенциал игрока против поля | справка `lap-potential`; полоса «идеал ↔ живой игрок» НЕ измерена | balance |
| 14–15 | Езда бок о бок ↔ перестановки (`OFFLINE_COST`) | сейчас 0.14; выше валит `tower` | race-ai |
| 16–17 | Логика обгона ИИ | 2 шага сделаны (v1.15.78, .80); момент атаки и VSC-пары — открыто | race-ai |
| 19 | «Один болид на всех» | посчитано, не принято; куски (а) `track.grip`, (б) тормоза как доля | balance |
| 20 | Соперник не быстрее предела точки | посчитано, не принято (дорого) | balance |
| 22 | Откуда разница выигрываемости трасс | закрыто: повороты 30–80 м | balance |
| 23 | ИИ по настоящей гоночной линии | посчитано; под VSC 9 → 30 спорных, ждёт п.16 | race-ai |
| 24 | Рельеф | не делать для Майами; цели — Монако, Сузука, Венгрия | tracks |
| 26 | Лёгкие режимы выигрываются с отрывом | посчитано, отложено владельцем | balance |
| 27 | Хунгароринг: (а) контур на середине дороги — сделано v1.16.4; (г) шум контура сглажен — сделано v1.16.5; grip, runoff, этап 3 | решение владельца | tracks |
| 28 | Хвосты `neutral`: мерка «срезать по траве» и правило «не приближаться» по скорости | отложено владельцем | race-ai |

### Проверено и ОТВЕРГНУТО — не предлагать заново

- Зоны атаки (обгон только в торможении) — быстрый болид застревает в очереди (v1.15.68).
- `OFFLINE_COST` > 0.14 — валит `tower`/`charge`; 0.06 валит `charge`.
- Связывать под флагом пару, идущую вровень — запирает насмерть (игрок 40 с в зоне).
- Общий абсолютный потолок под флагом — забракован арифметически (v1.15.37/42).
- Мерка «не держит темп» через долю темпа поля — выбирает трассу, а не правило.
- Отступной клапан «если наложились — бить как раньше» в пределе сближения — возвращает обвал.
- Расширение окна «в моей полосе» под вставшего игрока — хуже.
- Ворота «задний ход только вне трассы» — три промаха (v1.15.58).
- Смягчённая обрезка скорости ИИ окном ±2 точки — не чинит (п.20).
- Потолок ИИ на Профи 0.995 — почти пустой рычаг, двигает якорь (п.19в).
- «Держать строй вдали от торможения», «заходить с большего зазора», «поздний тормоз
  с платой за апекс», «нырок внутрь вместо `freeSide`» — все хуже (п.16).
- Лапласова «идеальная линия» — минимизирует длину, непроходима; линия — `tools/raceline.js`.
- Общее сглаживание барьера всем трассам — двигает Монцу/Сильверстоун, не чиня (v1.15.52).
- Защита зоны вылета долей радиуса — не чинит Майами, портит Монреаль (v1.15.89).
- База радиуса 20 м «пряталки не нужны» — лента складывается 174° (v1.15.91).
- Переменный шаг нарезки онбоарда по дистанции — не нужен (время уже даёт кривизну).
- Сверка поребриков/ширины зон по схеме гонки — схема рисована от руки, 7–9 м ошибки.
- Двухстрочная надпись в атласе — убрана (v1.16.1).
- Привязка машины игрока к команде / сцепления к силе пилота — отложено владельцем.
- Ослабить струю ниже 0.08 при ДЛИНЕ 50 м — поле перестаёт растягиваться (при 90 м 0.06 ок).

## 11. Журнал: последние сборки

- **v1.16.5** — опорные точки контура Хунгароринга сглажены (сдвиг ≤ 1 м): кромка и поребрик
  в T2, T8-9 больше не «прыгают»; поул −0.09…0.11 с, 9 клеток якоря и отпечаток переписаны.
- **v1.16.4** — контур Хунгароринга пересажен на середину дороги (остаток ±0.8 м вместо ±5);
  поул +0.09…0.29 с, 9 клеток якоря и отпечаток трассы переписаны, остальные не дрогнули.
- **v1.16.3** — Хунгароринг открыт в меню (снят `hidden`). `neutral` показал провал 5 м —
  пара бок о бок, была и на v1.16.2 (слепое пятно пробника).
- **v1.16.2** — седьмая трасса Хунгароринг, этап 1: линия старта по снимку + арифметике
  дистанции + решётке openf1 (0.3 м); контур гуляет от середины дороги на ±5 м.
- **v1.16.1 / v1.16.0** — реклама на бортах Монцы через общий атлас, 0 вызовов.

Чек-лист на v1.16.x: 20 из 22; красные `traffic` (Монца/Новичок 44.3 %, Монреаль/Норма
28.7 и 21.2) — теми же числами десять сборок подряд — и `neutral` (пары бок о бок; с v1.16.5
ещё строка «за кромкой под VSC» Хунгароринга — огрех мерки, разбор в журнале).
Полный журнал: `docs/notes/journal/` (новые записи сверху, файлы по диапазонам версий).

## 12. Как работать с владельцем

- **Всё по-русски**, и ответы, и рассуждения. Английскими остаются только цитаты из кода и вывода.
- **Он не программист**, кода не читает, проверяет на устройстве, объясняет словами и скриншотами.
- **Образец разбора:** что хотели → что НЕ изменилось и почему это по устройству →
  что изменилось и механизм бытовыми словами → короткая таблица → варианты с ценой.
- **Спрашивай до правки**, если она меняет ощущение, сложность или что-то двигает.
- **Меряй, а не гадай.** «Починено» — только после замера.
- **Когда расчёт спорит с устройством, прав владелец** — ищи ошибку в расчёте. У расчёта
  «возможно/невозможно» обязана быть полоса неопределённости.
- «Число прыгает от выборки» — гипотеза, а не объяснение: прогони прошлые сборки.
- Признавай неудачи прямо; честный откат дешевле.
- **Теги:** сперва попробовать `git push --dry-run` тега; при 403 — дать ссылку
  `https://github.com/lapsar/apex26/releases/new?tag=ИМЯ&target=ПОЛНЫЙ_SHA` (40 знаков,
  вручную в списке Target SHA не ищется). **В конце КАЖДОЙ работы** одной строкой: какой тег
  на какой коммит — после слияния это коммит в `main`, а не ветки.
- **PR сливать обычным merge, НЕ squash**; каждая версия — свой коммит (иначе тегу не на что встать).
- Каждая отданная сборка — в `archive/v<версия>.html`, содержимое не правится никогда.
  Версии v1.15.66–72 в репозитории нет вовсе. Судить о тегах по истории
  (`git log --full-history`), а не по `archive/`.
- Подробности: `docs/notes/owner.md`.

## 13. Регрессионный чек-лист

Одна команда: `cd tools && npm install` (один раз), затем `node tools/run-all.js`
(код возврата 0/1). **Пороги и эталоны пробников под правку не подгонять.**

- `pole` — 63 клетки якоря до тысячной; `print` — отпечаток всех трасс до сантиметра.
- `quali` — разброс ±0.2 % симметричен, слабый в топ-6 и лидер ниже P11 — 0 %.
- `race`, `outro`, `overtake`, `grid`, `startsync` — гонка добегает, автопилот после финиша,
  объезд стоящего на старте, решётка 22/22 и разметка, никто не трогается до фонарей.
- `wall`, `solid`, `clear`, `materials`, `load` — изломы < 75°, сквозь борт не проехать
  (в т. ч. в разрывах ленты), окружение не пересекает полотно и не стоит внутри барьера,
  стойки щитов за отбойником, трибуны не ближе 1.5 м по построенной геометрии, UV/цвета
  после склейки, меши/вызовы в бюджете.
- `neutral` — сход, флаги, плашки, объезд вставшего игрока за 40 с в трёх режимах;
  гонять и на `--seeds=3,5,13,23,42`. Плюс справка `neutral-audit` для правок темпа ИИ.
- `aiover`, `charge` (полной гонкой), `traffic` (6 зёрен), `tower` (6 зёрен) — обгоны ИИ,
  темп превращается в места, трафик вокруг игрока, башня не мельтешит. `aiover` гоняет
  один круг — правки очереди сверять ещё `abreast-audit` и полной гонкой.
- `fastlap`, `taps`, `dispose`, `audio` — лучший круг, тапы и залипание кнопок (цель касания
  бывает ТЕКСТОВЫМ узлом), освобождение видеопамяти (не трогать `ENVSHADOW`), звук после
  блокировки iPad.
- **Проверка на устройстве владельцем обязательна.**
- Полная версия с объяснениями «что ловит и почему»: `docs/notes/checklist.md`,
  запуск и оговорки — `tools/README.md`.

## 14. Репозиторий

```
index.html        игра — единственный продукт
CLAUDE.md         это ядро
archive/          отданные сборки v<версия>.html (не править)
docs/             брифы модели и окружения; docs/notes/ — вся подробная память
tools/            пробники (probe-*.js), справки (*-audit.js, raceline.js, lap-*.js…),
                  harness.js, run-all.js, съёмка (shot-*.js), замеры трасс (*-scan/),
                  исходники разметки SCENERY_*.js (истина — в index.html)
```

Пробники выполняют игровой скрипт из `index.html` в `vm` (ничего экспортировать не нужно),
`Math.random` подменяется зерном; опытные сборки — через `APEX_INDEX`. Сравнивать сборки —
только двумя отдельными процессами (файл выбирается при `require`).
Полное дерево с описанием каждого инструмента: `docs/notes/repo.md`.

## 15. Ведение памяти: что куда записывать

Поддерживай память сам, без отдельной просьбы, **в том же коммите**, что и правка.
Принцип: **журнал — это история («что сделали и когда»), тематический файл — это
нынешняя правда («как устроено и что намерено»), ядро — только правила и указатель.**
Один результат обычно пишется в два места: запись в журнал + правка тематического файла.

| Что появилось | Куда записать |
|---|---|
| Новая сборка (что сделано, числа, чек-лист) | `docs/notes/journal/` — сверху файла нужного диапазона версий (новый диапазон — новый файл) + одна строка в §11 ядра (там 3–5 последних) |
| Замер без правки игры (вопрос владельца, справка) | тематический файл по смыслу + короткая запись в журнал с датой |
| Баланс, темп ИИ, якорь, режимы, запас игрока | `docs/notes/balance.md`; новый якорь — ещё и таблица §4 ядра |
| Флаги, VSC, сходы | `docs/notes/neutralisation.md` |
| Физика игрока, руль, отбойник, задний ход | `docs/notes/game.md` |
| Всё про конкретную трассу (замеры, разметка, ловушки) | `docs/notes/tracks/<трасса>.md` (новая трасса — новый файл) |
| Общий урок разметки, правило для всех трасс | `docs/notes/tracks/overview.md` + строка в §8 ядра, если это запрет |
| Источник данных (openf1, TUMFTM, OSM, снимки, онбоард) | `docs/notes/sources.md` |
| Меши, вызовы, атлас, видеопамять | `docs/notes/performance.md`; изменились бюджеты — таблица §3 ядра |
| Известный дефект, который не чиним | `docs/notes/limitations.md` + строка в §9 ядра |
| Открытая задача, посчитанный вариант, решение владельца | пункт в `docs/notes/open-tasks/<тема>.md` + строка в таблице §10 ядра |
| Отвергнутая замером идея | разбор — в тематический файл, строка — в «Проверено и отвергнуто» ядра |
| Новый пробник или изменённый порог | `docs/notes/checklist.md` + строка в §13 ядра; инструмент — `docs/notes/repo.md` |
| Новое правило работы с владельцем | §12 ядра (и подробности в `docs/notes/owner.md`) |
| Новая сборка в архиве | дерево в `docs/notes/repo.md` |

- **Ядро держать коротким (не больше ~500 строк).** Замеры, таблицы, разборы — только в `docs/notes/`.
- Не дублируй: обновляй существующий пункт вместо нового. Устаревшее не стирай,
  а помечай, до какой версии действовало (так было и раньше).
- Новый файл в `docs/notes/` — добавь его в указатель §16.

## 16. Указатель: что читать перед работой

| Перед работой над… | Прочитай |
|---|---|
| темпом ИИ, сцеплением, тормозами, режимами, якорем | `docs/notes/balance.md`, `docs/notes/open-tasks/balance.md` |
| флагами, VSC, сходами, дистанцией под флагом | `docs/notes/neutralisation.md` |
| обгонами, трафиком, ездой бок о бок, башней | `docs/notes/open-tasks/race-ai.md`, `docs/notes/limitations.md` |
| физикой игрока, рулём, отбойником, задним ходом | `docs/notes/game.md`, `docs/notes/limitations.md` |
| окружением конкретной трассы | `docs/notes/tracks/<трасса>.md` + `tracks/overview.md` |
| добавлением новой трассы | `docs/notes/tracks/overview.md`, `docs/notes/sources.md` |
| онбоардом, openf1, TUMFTM, OSM, спутником | `docs/notes/sources.md` |
| рекламой, мешами, вызовами отрисовки | `docs/notes/performance.md` |
| кнопками, звуком, видеопамятью, интерфейсом | журнал (grep по версии/слову) |
| «а что было с X?» | `grep -rn "X" docs/notes/` |

**Где теперь разделы прежнего CLAUDE.md** (на них ссылаются записи «§4», «§10 п.16»):

| Было | Теперь |
|---|---|
| §1, §2, §5, §6 | `docs/notes/game.md` |
| §3 | `docs/notes/performance.md` |
| §4 / §4-bis | `docs/notes/balance.md` / `docs/notes/neutralisation.md` |
| §7, §8 | `docs/notes/tracks/*.md` (общее — `overview.md`), источники — `docs/notes/sources.md` |
| §9 / §9-bis | `docs/notes/limitations.md` / `docs/notes/tracks/overview.md` |
| §10 п.3, 13, 19–22, 26 | `docs/notes/open-tasks/balance.md` |
| §10 п.4, 5, 7–12, 14–18, 23 | `docs/notes/open-tasks/race-ai.md` |
| §10 п.1, 2, 6, 24, 25, 27 | `docs/notes/open-tasks/tracks.md` |
| §11 | `docs/notes/journal/` (по диапазонам версий) |
| §12, §15 / §13 / §14 | `docs/notes/owner.md` / `checklist.md` / `repo.md` |
